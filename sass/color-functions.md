# Color Functions

```css
$newColor: lighten($color, 10%);
$newColor: darken($color, 10%);

$newColor: saturate($color, 10%);
$newColor: desaturate($color, 10%);

$newColor: fade_in($color, .1);
$newColor: fade_out($color, .1);

$newColor: invert($color);
$newColor: complement($color);
```

